package com.rciliberto.blabber.controller;

import com.rciliberto.blabber.model.Generator;

/**
 * The Controller for a {@link Generator}.
 * {@link GeneratorController#start()} renders the view.
 */
public interface GeneratorController {

  /**
   * Starts the controller and renders the view.
   */
  void start();
}
